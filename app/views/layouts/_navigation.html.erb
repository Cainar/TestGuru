<h1>
  <%= link_to 'TestGuru', root_path %>
</h1>

<% if logged_in? %>
  <p class="nav user">Welcome,&nbsp<b><%= current_user.email %></b>&nbspGuru</p>
  <p class="nav user">
    <%= link_to 'Log Out', logout_path,
                          data: { confirm: 'Are you sure?' } %>
  </p>
<% end %>

<p class="nav user">
  <%= link_to 'Log In', login_path %>
</p>

<p class="nav user">
  <%= link_to 'Sign Up', signup_path %>
</p>
