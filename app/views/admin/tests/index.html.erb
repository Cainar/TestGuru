<h1><% t('.header') %></h1>

<p>
  <%= link_to t('.link'), new_admin_test_path %>
</p>

<% if @tests.present? %>
  <table>
    <tr>
      <th><%= Test.human_attribute_name("title") %></th>
      <th><%= t('.number') %></th>
      <th colspan='3'><% t('common.actions') %></th>
    </tr>

    <%= render @tests %>
  </table>
<% end %>

<p>
  <%= link_to t('.gist_list'), admin_gists_path %>
</p>
