<h1><%= @test.title %></h1>

<p>
  <%= link_to "#{t('common.new')} #{Question.model_name.human}", new_admin_test_question_path %>
</p>

<% if @test.present? %>
  <table>
    <tr>
      <th><%= Question.model_name.human %></th>
      <th colspan="3"><%= t('common.actions') %></th>
    </tr>

    <%= render @test.questions %>
  </table>
<% end %>

<%= link_to t('common.back'), tests_path %>

